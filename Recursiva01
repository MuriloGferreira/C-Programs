#include <stdio.h>
#include <stdlib.h>

int fib( int n){

if(n<=2){

  return 1;
}else

return fib(n-1)+fib(n-2);
}


int main(void) {

int resp, N;
  
  printf("Digite o número:");
  scanf("%d", &N);

  resp = fib(N);

  printf("O Fibonacci de [%d] é: %d", N, resp);

  return 0;
} 
